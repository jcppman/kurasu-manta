# Application settings
TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_KEY=

# Database configuration
# Default is PostgreSQL. Set DB_CONNECTION=sqlite to use SQLite instead
DB_CONNECTION=

# PostgreSQL settings (used when DB_CONNECTION is empty or 'postgres')
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=app

# SQLite settings (used when DB_CONNECTION=sqlite)
DB_FILE=tmp/db.sqlite3

# API Keys
OPENAI_API_KEY=
